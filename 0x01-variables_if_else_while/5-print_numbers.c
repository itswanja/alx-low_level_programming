#include <stdio.h>
/**
 * main - Program that prints numbers of base 10 from 0 to 9
 * Return: 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 0; 1 < 10; i++)
	{
		printf("%d\n", i);
	}
	return (0);
}
